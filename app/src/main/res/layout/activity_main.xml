<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- Dynamic gradient background from :lottie module (always visible) -->
    <com.voicerobot.lottie.widget.GradientBackgroundView
        android:id="@+id/gradientBg"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:gbvIntensity="1.0"
        app:gbvSpeedPxPerSec="220" />

    <!-- Top-right toggle '字' (always visible) -->
    <TextView
        android:id="@+id/tvToggleTextPanel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="20dp"
        android:includeFontPadding="false"
        android:text="字"
        android:textColor="@android:color/white"
        android:textSize="28sp"
        android:fontFamily="@font/alimamadaoliti"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Robot area: avatar + chat overlay (overlay covers avatar only) -->
    <FrameLayout
        android:id="@+id/robotArea"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/waveform"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.airbnb.lottie.LottieAnimationView
            android:id="@+id/robotAvatar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            app:lottie_autoPlay="true"
            app:lottie_loop="true"
            app:lottie_rawRes="@raw/bot_standby" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvConversation"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone"
            android:paddingTop="56dp"
            android:paddingBottom="16dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:clipToPadding="false"
            tools:listitem="@layout/item_chat_message" />

    </FrameLayout>

    <!-- Waveform (three dots) -->
    <com.voicerobot.lottie.widget.WaveformView
        android:id="@+id/waveform"
        android:layout_width="0dp"
        android:layout_height="36dp"
        android:layout_marginBottom="14dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottomActions" />

    <!-- Bottom actions: 只调整大小与位置，不改图标本身 UI -->
    <LinearLayout
        android:id="@+id/bottomActions"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="48dp"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingStart="32dp"
        android:paddingEnd="32dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/btnMic"
            android:layout_width="76dp"
            android:layout_height="76dp"
            android:contentDescription="mic"
            android:src="@drawable/ic_mic_normal" />

        <Space
            android:layout_width="44dp"
            android:layout_height="1dp" />

        <ImageView
            android:id="@+id/btnVideo"
            android:layout_width="76dp"
            android:layout_height="76dp"
            android:contentDescription="video"
            android:src="@drawable/ic_video_call" />

        <Space
            android:layout_width="44dp"
            android:layout_height="1dp" />

        <ImageView
            android:id="@+id/btnEnd"
            android:layout_width="76dp"
            android:layout_height="76dp"
            android:contentDescription="end"
            android:src="@drawable/ic_call_end" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
